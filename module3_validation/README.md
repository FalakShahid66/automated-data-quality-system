Module 3 — AI Validation, Anomaly Detection & Rule Engine.

Applies rule-based validation (format, range, consistency checks), then hybrid fraud detection: XGBoost (supervised, catches known fraud patterns) + Isolation Forest (unsupervised, catches novel/unusual patterns). Scores error and anomaly severity, produces an overall dataset health score.

Output: validation_report.json
