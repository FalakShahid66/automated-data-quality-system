Module 4 — Integration, Orchestration & Production Engineering.

Combines Modules 1-3 into a single configurable pipeline (profiling -> cleaning -> validation), run via CLI. Includes YAML config, logging, automated tests, and Docker packaging.

Usage: python pipeline.py --input data/raw/transactions.csv --output reports/

Output: end-to-end pipeline (CLI + Docker)
